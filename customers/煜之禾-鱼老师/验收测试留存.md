# 验收测试留存 · 煜之禾-鱼老师(爆创)

> 测试日期:2026-07-16 ~ 07-17 · 测试环境:打包安装版(mac dmg,BUILD 47-50)
> 方法:对照客户《需求评审报告 v3.2》十六条端到端验收标准,逐条在**打包安装的应用**上真跑
> (真实 TikHub/LLM/ASR/飞书调用,非 mock);CLI 用 `od baokuan/od monitor` 直测;UI 用应用内
> 已认证页面发起同源请求(与界面按钮完全同一端点)+ 截图。每条含:操作、实际输出、结论。
> 测试期间发现的缺陷全部当场修复并复验,列于文末「缺陷记录」。

## 一、十六条验收逐条结果

### 验收1 飞书新增关键词并启用 → 系统读取到 ✅
- 操作:`od monitor add --keyword "ZZ定时验证词" --platforms 小红书 --min-threshold 999999`
  → 飞书监控配置库出现该行(record_id recvpxZjqRO9j5,启用);再 `od baokuan scheduled`
- 实际输出:`{"runs":[{"keyword":"ZZ定时验证词","candidates":0,...}]}` ——启用项被读取并调度;
  禁用的种子行「男性情感」正确未跑(启用过滤生效)。candidates=0 是高阈值故意滤掉(控测试成本)。
- 清理:测试项已删。

### 验收2 新增竞品账号 → 按指定时间窗采集写入原始库 ✅
- 操作:`od baokuan account --name "https://www.douyin.com/user/MS4wLjABAAAAuLZ..." --window 1d --platforms 抖音`
  (账号=真实抖音「宅男相亲记」)
- 实际输出(引擎日志):`[竞品账号] douyin 窗口外跳过 23 条(时间窗 1d)` ——真抓到 23 条作品、
  全部按 1 天窗过滤(该账号近1天无新作)。窗口过滤此前**不生效**(缺陷#3),当场修复后复验。

### 验收3 同一内容重复抓取:不重复建主记录 + 更新最新数据快照 ✅
- 操作:同关键词「脱单」抖音采集跑两遍。
- 实际输出:第二遍引擎日志 `重复内容 11 条:不重建主记录,复用既有原始库记录`;本地快照表
  同一内容最多 **9 期快照**(douyin_7659631504426896659);重复时最新互动数据刷回原始库主记录。
- 注:此能力为测试中发现缺陷#4 后新实现并复验。

### 验收4 输出爆款总分/分项/等级/评分理由 ✅
- 操作:`od baokuan collect --keyword "脱单" --platforms 小红书 --json`
- 实际输出(首条):流量爆款分 73.3 / 精准意向分 100 / 热度 B /
  评分理由「流量73.3(B)/意向100.0(S);数据热度0.88、增速缺失、账号超额缺失」——总分+分项+等级+理由齐全。

### 验收5 单链接完整链路:文案提取/ASR、拆解、素材召回、脚本、回写飞书 ✅
- 操作:`od baokuan link "https://www.iesdouyin.com/share/video/7597366581819149602/"`(9万赞真实爆款)
- 实际输出:`{"platform":"douyin","content_id":"douyin_7597366581819149602","generated":true}`;
  飞书三表真实写入且**内容与该视频完全匹配**——
  拆解库③:核心矛盾「低薪直男想要恋爱但条件差」;
  审核库④:标题「月薪3000还想谈恋爱?直男相亲翻车实录」+ 60/90秒完整脚本;
  复盘库⑤:复盘结论+评分理由。
- ASR:87 秒短视频转写出 **592 字真实口播对话**(缺陷#5 修复后)。
- 更新(2026-07-17):小红书单链接已彻底修复——根因是图文笔记误用视频详情端点(返回推荐流);现按 图文(get_image_note_detail)/视频/精确(web_v3+xsec_token) 三端点级联+id校验,真机复验同链接「命中本条」、拆解原文逐字一致
  + 专属报错引导改用关键词采集(缺陷#2)。

### 验收6 飞书标「重新生成」→ 出新版本保留旧版 ✅
- 操作:审核库该记录状态改「重新生成」→ `od baokuan regenerate --json`
- 实际输出:`{"regenerated":1}`;审核库变两行——
  版本1 状态「已重生成」(保留)/ 版本2 新标题「月薪3000的直男,相亲现场有多惨?」状态「待审」。

### 验收7 更换风格版本 → 脚本语气/结构随之改变且可追溯 ✅
- 操作:风格画像库置「ZZ测试-知性温和风」为唯一生效(禁用词含"兄弟") → regenerate 出 v3。
- 实际输出对比:
  v1/v2(旧风格):「**兄弟们**,我最近去相亲了…」
  v3(知性温和风):「**朋友们,我理解**很多收入不高的男生在相亲时会紧张…**其实**…」
  ——常用词切换、禁用词避开,v1/v2/v3 全保留可追溯。测试风格已删,恢复 v1-男性情感口播 生效。

### 验收8 上传参考封面 → 生成类似视觉语言新封面,中文标题准确 ✅
- 操作:取真实抖音爆款封面为参考 → `od baokuan cover gen --title "月薪3000的直男,相亲现场有多惨?"
  --ref /tmp/ref_cover.jpg --platforms douyin,bilibili`
- 实际输出:抖音竖版 1080×1920 + B站横版 1920×1080 成图;参考图经 Seedream **图生图**条件生成
  (同类视觉语言);中文标题 Pillow **程序叠字**(100% 不错字);溢出校验 overflow=False。
- 如实说明:客户 ARK 账号未开通任一 vision 模型(126 个逐一探测均 404),「参考封面结构化解析」
  降级为主色提取;在方舟控制台开通任一 doubao vision 后自动升级(VISION_MODEL 已配好+防呆)。

### 验收9 修改封面标题 → 不重出背景,仅重渲染文字出新版本 ✅
- 操作:`od baokuan cover rerender --bg <上一步*_bg.png> --title "被拒17次后,我终于悟了相亲的真相"`
- 实际输出:**0 秒**完成(不调图像模型);新旧图**顶部区域像素指纹一致**(md5 相同,背景确未重出);
  新标题渲染正确、无溢出。

### 验收10 总控停止后,主定时任务与飞书主流程仍独立运行 ✅
- 操作:app+daemon+web **全部杀死**(0 进程残留)→ 纯最小 PATH 直跑引擎 `--scheduled`
- 实际输出:`{"runs":[]}`(监控库当时无启用项,正确)、0 错误——核心自动化完全不依赖桌面端/总控。
  客户可用 cron 直调引擎或 `od baokuan scheduled`。

### 验收11 主服务故意失败 → 切备用模型/进失败队列,不丢记录 ✅
- 模型 fallback:把主模型改成假名 `bogus-model-not-exist` → 引擎日志
  「主模型 bogus-model-not-exist 失败(RetryError),切备用 deepseek-chat 重试」→ 回复「成功」。
- 失败队列:假链接 link 必失败 → `od baokuan failed` 显示
  `#1 [link] RuntimeError: 详情返回空数据(无内容ID),aid=999…`;
  **飞书复盘库同步出现「错误摘要」行**(客户在飞书直接看到原因);
  `od baokuan retry` 重跑分发执行(该假链接按设计再失败、留队列,不重复入队——缺陷#7 修复后)。
- 成本台账:`od baokuan cost --days 7` → 「llm: 3 次调用, 227 tokens / tikhub: 9 次调用」
  (LLM tokens/ASR次/图像张/TikHub次 四类按任务归因,支持按天分布)。

### 验收12 接口/飞书写入故意失败 → 日志/飞书可见具体原因 ✅
- 操作:PATH 剥掉 lark-cli(客户机真实故障形态)→ radar 采集。
- 实际输出:`feishu_synced: False`(前端红字来源)+ 日志
  「飞书数据中心暂不可用(未连接/未建表)，本轮跳过回写、只产出选题：FileNotFoundError: 'lark-cli'」
  + **选题照常产出 8 条**(失败不丢任务)。

### 验收13 重启后自动恢复 / 备份迁移 ✅(代码面)
- 开机自启:`od baokuan autostart status→on→off` 全周期通过——on 后
  `~/Library/LaunchAgents/com.workbuild.autostart.plist` 写入且指向 WorkBuild.app,off 后删除。
- 备份:`od baokuan backup` → 711MB tar.gz,含 引擎数据(去重库/成本台账/失败队列)+ 全部配置
  .env + 应用数据(知识库/文章/账号 SQLite);排除可再生的引擎运行时(新机装应用自动重建)。
- 恢复:`ops.py restore --from <tar>` → 探针目录还原 64 文件,app.sqlite ✓。
- 说明:「一页说明启动」属于交付文档(见验收14-16 待办)。

### 验收14/15/16 三本手册+分层流程图 ⏳ 未做(唯一残留)
- 《从零搭建全流程手册》《系统全参数配置对照表》《常见报错排查手册》+ 4 类流程图 PDF——
  纯文档交付物,尚未编写。功能面 13 条已全部通过。

## 二、界面 ↔ 后端 对应性测试(打包应用内)

| 界面功能 | 后端端点 | UI同源请求实测 | CLI 等价命令 |
|---|---|---|---|
| 账号页·运维卡片·近7天成本 | GET /api/baokuan/cost | kinds=2 ✓ | od baokuan cost |
| 账号页·运维卡片·失败队列+重跑 | GET /failed + POST /retry | rows=1 ✓ | od baokuan failed / retry |
| 账号页·运维卡片·一键备份 | POST /api/baokuan/backup | 711MB 产物 ✓ | od baokuan backup |
| 账号页·运维卡片·开机自启 | GET/POST /api/baokuan/autostart | enabled 读写 ✓ | od baokuan autostart |
| 账号页·定时监控卡片(增删改) | /api/feishu/monitor | 加/删真写飞书 ✓ | od monitor add/rm/list |
| 账号页·全局参数 | /api/feishu/system-config | TopK 改9→读9→改回8 ✓ | od monitor config |
| 知识库页(个人/企业) | /api/media-studio/*/knowledge | 建→飞书素材库/风格库出现,删→同步删 ✓ | (创作动线) |
| 创作台·标记完成 | mark-published | 审核库④出现 ✓ | — |
| 创作台·复盘 | push-review | 复盘库⑤出现 ✓ | od studio push-review |
| 选题(真抓爆款) | collect-score | 12条候选+评分 ✓ | od baokuan collect |

**视觉证据**(本目录`测试证据/`):
- `运维卡片-账号页.png`——打包应用实拍:近7天成本(227 tokens/10次)、失败队列#1可见+重跑按钮、
  备份/自启按钮、系统配置(TopK 8/检测频率120等,来自飞书实时数据)。
- `监控配置卡片-账号页.png`——定时监控+全局参数卡片。

## 三、飞书数据中心 10 表连通性(逐表)

| 表 | 方向 | 实测 |
|---|---|---|
| ①爆款内容原始库 | 写+更新 | 采集写入/重复刷新数据 ✓ |
| ②今日爆款选题池 | 写 | 评分选题+关联① ✓ |
| ③爆点拆解库 | 写 | 单链接拆解+关联① ✓ |
| ④成品内容审核库 | 写+读+更新 | 脚本v1/v2/v3、重新生成状态机 ✓ |
| ⑤发布复盘库 | 写 | 复盘+成本+**错误摘要**(失败上报) ✓ |
| ⑥监控配置库 | 读+写 | 界面/CLI增删改,scheduled 读取调度 ✓ |
| ⑦我的素材库 | 写+读 | 知识库双写/删除同步/RAG召回 ✓ |
| ⑧风格画像库 | 写+读 | 单例生效切换、控脚本语气(验收7) ✓ |
| ⑨系统配置表 | 读+写 | 界面改值→引擎热更(启动日志「已生效」) ✓ |
| ⑩Prompt与模型策略库 | 预留 | 已建表未接线(如实说明) |

## 四、缺陷记录(本轮真机测试发现并修复,含修复验证)

| # | 缺陷 | 影响 | 修复+复验 | commit |
|---|---|---|---|---|
| 1 | 采集平台传中文名(小红书/抖音)引擎 KeyError→0条 | od baokuan/监控库全废 | 中文名+代号双认;复验 小红书 0→20条 | 4907896b |
| 2 | 小红书单链接拿推荐流内容写飞书(内容全串) | 写错内容 | id守卫拒收+专属报错;复验同链接拒绝、零污染 | 95db3a68 |
| 3 | 竞品采集 time_window 只打日志不生效 | 假窗口+成本炸弹 | 真过滤;复验「窗口外跳过23条」 | d961e171 |
| 4 | 重复抓取不更新快照;radar 重复建主记录 | 违验收3 | 快照先记+主记录去重;复验「重复11条不重建」 | d961e171 |
| 5 | 主链路视频ASR全部静默失效(火山拉不动抖音CDN) | 拆解/脚本只靠标题 | 本地音频base64路径;复验87s视频592字真口播 | 17d4a302 |
| 6 | 单链接空壳数据以 content_id="douyin_" 继续跑(strip("_")判不出) | 垃圾数据入库 | 判ID后缀;复验假ID→拒收入队 | 3df3af7e后续 |
| 7 | retry 再失败重复入队,队列越滚越多 | 队列膨胀 | 重试期不入队;复验队列稳定1条 | 同上 |
| 8 | VISION_MODEL 误配成图像生成模型(404) | 封面解析废 | 改真vision模型+代码防呆 | c6af096e |
| 9 | ASR轮询36s上限对长视频全灭 | 长视频转写废 | 时长自适应预算 | 17d4a302 |
| 10 | router「备用模型fallback」只有注释没实现 | 验收11缺 | 真fallback;复验bogus→切deepseek-chat成功 | 3df3af7e |
| 11 | 备份含符号链接致 restore 崩;备份重复打包可再生运行时 | 恢复失败 | 排除+跳过;复验711MB备份/64文件还原 | 3df3af7e后续 |

## 五、遗留与如实说明

1. **验收14-16 交付文档**(三本手册+流程图)未做——唯一功能外残留。
2. 小红书**单链接**已修复(图文误用视频端点是根因;三端点级联+id校验,图文/视频均精确命中本条,真机复验通过);极少数取不到=笔记已删或链接不完整。
3. 封面「结构化视觉解析」需客户在方舟控制台开通任一 doubao vision 模型(一键),未开通时用
   图生图+主色提取,验收8 效果不受影响。
4. Prompt与模型策略库⑩为预留(Prompt 仍在代码内,系统配置表⑨的「默认模型.high」已可在飞书改)。
5. 测试数据处置:ZZ 前缀测试记录全部清理;审核库保留「直男相亲」v1/v2/v3 真实生成链作演示;
   开发库(base C1xtba...)历史测试数据不影响交付——正式交付时为客户 provision 全新 base。

---

## 终包复验(2026-07-17 上午,最终交付包)

用户质疑推动的两处根因修复,重新打包后在**打包环境**复验:

| # | 测试 | 命令 | 结果 |
|---|---|---|---|
| F1 | 小红书图文单链接(修复前串台той条) | `od baokuan link https://www.xiaohongshu.com/explore/681a0ce1...` | ✅ content_id 与请求完全一致,generated:true;引擎日志「小红书详情命中本条(get_image_note_detail)」;飞书拆解③原文逐字一致 |
| F2 | 封面参考图视觉解析 | `od baokuan cover analyze --ref <真实抖音封面>` | ✅ 来源:vision(非降级),输出构图/色彩/字体层级/人物位置/留白/情绪,约8秒 |

包内容抽查:三端点级联(_xhs_note_detail)✓ 图文端点✓ /responses✓ VISION_MODEL=ep-2026...✓。
两处修复均由用户质疑推动定位真根因:①小红书串台=图文笔记误用视频详情端点(非"平台限制");
②vision 404=用错 /chat/completions 端点+模型名(非"客户没开通",应走 /responses+接入点ID)。

---

## 2026-07-17 稳定性挂测前的三处深坑修复(v4 终包)

启用 3 天稳定性 cron 时首轮即暴露三个真问题,全部修复并实证后重打 v4 客户包(交付 dmg 已替换,md5 31c275acb97f39d2ff3592896ced87ff):

### 坑1:od CLI 长任务 5 分钟必断线(产品 bug)
- **现象**:`od baokuan scheduled` 跑到 301s 报 `UND_ERR_HEADERS_TIMEOUT` exit=3,daemon 端管线其实还在跑(白干)。
- **根因**:daemon 端点同步跑完管线才回响应头;Node fetch(undici)默认 headersTimeout=300s,先掐。
- **修复**:daemon 4 个长端点(scheduled/account/link/regenerate)改为先发 200 头+每 15s 写一个空格心跳(JSON 合法前导空白),结果出来再写 JSON 体;错误编码为 200+`{error}`,od CLI 的 post 守卫据此 stderr+exit 5,cron 统计的退出码语义不变。
- **实证**:修复后同一轮 **exit=0 dur=325s**(>300s 断线点),runs=[男性情感成长:候选11/处理5/生成5/归档6]。

### 坑2:app 升级会抹掉客户连好的飞书(产品 bug,交付级)
- **现象**:装 v3 后 runtime 引擎 `.env` 的 FEISHU_BITable_APP_TOKEN 被出厂空白覆盖,`od monitor list` 变"暂无监控项"(其实飞书行都在,是引擎查不到)。
- **根因**:`resyncEngineSourceIfStale` 把捆绑 `.env` 直接 cp 盖 runtime——客户每次升级 app,「连接飞书」写入的 token 全被抹掉,采集静默断。
- **修复**:`mergeBundledEnvPreservingRuntime`——runtime 已有键一律保留,只追加捆绑里新增的键(新版引擎仍能下发新配置默认值)。
- **实证**:写回 token → 删 `.od-engine-src-stamp` 强制再 resync → token **保留**(v3 行为会被抹回空白)。

### 坑3:多客户检出同名进程互杀 + IPC socket 全局共享(操作纪律)
- **现象**:裸 `pkill -f WorkBuild` 误伤另一客户检出(open-design/)正在跑的打包进程;两检出 app 同用 `/tmp/open-design/ipc/default/daemon.sock`,并行起会 EADDRINUSE/互掐(曾把进行中的请求 100s 处掐断)。
- **约定**(已写进 CLAUDE.md):清场按路径限定 `pkill -9 -f "自媒体营销/.tmp/tools-pack.*WorkBuild"`;验证用 pgrep(ps 会把中文路径转义,grep 中文必空);同一时刻只让一个客户包的 app 活着。

### 稳定性挂测状态
- crontab:`0 */2 * * * .../run-stability.sh`(已挂)。
- 首轮(v4 修复后):exit=0 / 325s / 候选11 处理5 生成5——日志 `customers/煜之禾-鱼老师/稳定性测试/stability-log.jsonl` 持续累计,3 天后汇总成功率。

---

## 2026-07-17 运行时隔离(用户要求:本机 3 客户包并存,绝不互相干扰)

- **本分支独占 namespace `baochuang`**:本地 app=`WorkBuild.baochuang.app`,数据目录 `.tmp/tools-pack/runtime/mac/namespaces/baochuang/data/`(1.0G 全量迁移自 default:sqlite/账号/license/引擎 .env+token 完整,venv 删除后由 provision 在新路径重建),IPC socket `/tmp/open-design/ipc/baochuang/daemon.sock`,端口动态互不占用。本机 `/tmp/open-design/ipc/` 下另有 default/social-auto/workbuild 三个 namespace 属于其他客户包,零接触。
- **产品级修复(daemon-url.ts)**:od CLI 显式钉死 `OD_SIDECAR_IPC_PATH` 后,探测失败**硬报错拒绝回退**跨 namespace 扫描。修复前实测:钉死探测失败会静默回退连到**另一个客户的 daemon**(HTTP 404 = 人家没这路由)——多客户机器上是串库级危险。负路径实测:钉到不存在的 socket → `daemon not reachable at pinned IPC endpoint ... refusing cross-namespace fallback`。
- **稳定性 wrapper 已钉死 socket**(run-stability.sh export OD_SIDECAR_IPC_PATH)。隔离后完整轮实测:**exit=0 / 221s / 候选4 处理4 生成4**(去重正常:上一轮归档过的 6 条重复未再入库)。cron `0 */2 * * *` 继续累计。
- 客户交付 dmg 不受影响(仍为已归档 v4,md5 31c275acb97f39d2ff3592896ced87ff);baochuang 仅是本机测试 lane。

---

## 2026-07-18 Windows 链路复核与 P0 修复实证(mac 经 ssh 远程操作 win 打包机 192.168.1.11)

win 智能体的 13 文件改动经逐 diff 审查后全部批准,由 mac 收 bundle 代推(win 机 HTTPS 无凭据):a5fb3fc8(GBK)/ba900ffe(win32 provision)/3873bafe(win license)。随后 mac 侧补修 4a2f11cb 并在 win 真机逐项实证:

- **开机自启 win 分支(修假成功)**:此前 win 上写 plist 到 `%USERPROFILE%\Library\`(没人读)还报成功。改为 HKCU Run 注册表。实证:`od baokuan autostart on` → `reg query` 出现 `WorkBuild REG_SZ "E:\...\WorkBuild.exe"`;`off` → 注册表值删除。
- **license 内嵌 win 安装包**:`OD_PACK_CUSTOMER=yuzhihe` 重打(536MB,日志「bundled license customer=煜之禾·鱼老师」)。静默重装→删 runtime license→重启,daemon 日志:「[license] 已从安装包内置授权完成首启安装」+「valid · 煜之禾·鱼老师 · 15 features · expires 2027-12-31」。
- **完整业务链路 win 首测**:`od baokuan collect --keyword 男性情感 --platforms 抖音` → 12 条真数据(评分/四榜单齐全);`od baokuan link <抖音分享链>` → `generated:true`(采→ASR→LLM③④⑤→写飞书全链,长任务心跳生效)。
- **tar 兜底 --force-local / Gemini 兜底人话报错 / 出厂 .env 核对(VISION_MODEL= 空行在、无 ep- 残留)** 均已进 4a2f11cb。
- **运维事实**:Electron 在 ssh 非交互会话起不住(daemon 写完日志随桌面进程退出),远程起 app 用 `schtasks /create /it + /run` 注入交互会话;win runtime .env 现指向 win 自建 base(BKOXbX...,总结里的 E0LHbn 已被后续建表替换,无数据丢失,.venv 仍为 2026-07-17 23:25 创建)。
- **交付 namespace 已拍板(2026-07-18)**:定死 `baochuang`。理由:客户/打包机将来会装多个不同客户的定制包,namespace 后缀让快捷方式/卸载项天然可区分。注册表卸载键 `WorkBuild-baochuang` 随之定死,后续升级包永远用同 namespace。当前 536MB 带 license 安装包即交付物。

### 2026-07-18 win 剩余烟测补齐(开机后续测)
- **成本报表**:`cost --days 7` 返回真实账目(昨晚测试的 ASR 1 次/LLM 4 次 4037 tokens/TikHub 5 次),win 记账正确。
- **失败队列**:`failed --json` 正常应答(见 /tmp 留档)。
- **封面生成**:`cover gen --title 三句话让男人主动改变 --platforms douyin` → ok:true,1080×1920 落盘,**mac 侧拉回图片肉眼验证**:中文黄字黑边叠字两行、无方框(微软雅黑字体链生效),背景另存 `_bg.png` 可复用重排版。
- **备份**:`backup` → `WorkBuild备份-20260718-100605.tar.gz`(2.3MB,engine-data + .env + daemon-data)。
- 运维:开机后远程起 app 复用 `schtasks /run /tn baochuang-start`(注入交互会话)。win 全部 od baokuan 面(collect/link/cost/failed/cover/backup/autostart)至此逐项实测完毕。

### 2026-07-18 win 平台维度补测(如实记录)
- **小红书 collect**:✓ 5 条真数据(TikHub 搜索)。
- **小红书 link**:三端点级联(web_v3→图文→视频)在 win 正确执行、GBK 安全、人话报错——测试用裸 explore 链接缺 xsec_token 故取不到本条(与 mac 同行为;mac 验收已用带 token 分享链接验过成功路径)。win 成功路径待一条带 token 的分享链接补验(30 秒)。
- **scheduled 真测**:加启用监控「男性情感成长·抖音」后整轮:候选 7→处理 5→生成 5→重复归档 2,LLM 20 calls——定时全链在 win 闭环,去重同样生效。
- **win 未实测面(需登录态/人手,远程不可代)**:快手/B站/视频号的浏览器采集与全平台存草稿(app 内无任何账号登录态,扫码只能真人来)、公众号/知乎 UI 流程。以上在 mac 打包版历史验收中全部实测通过;win 引擎/CLI/daemon 面(平台无关)已全测。

### 2026-07-18 稳定性 cron 假死修复 + CLI npm 包
- **cron 轮全挂根因**:cron PATH 只有 /usr/bin:/bin,homebrew 的 node/timeout 找不到→每轮 exit 127(手动跑带全 PATH 测不出)。wrapper 顶部补 `export PATH=/opt/homebrew/bin:...`。用 `env -i PATH=/usr/bin:/bin` 模拟 cron 环境真跑一轮:exit 0/227s/候选4→生成4 ✓。3 天窗口自此起算。mac 爆创实例曾自行退出,已重启(机器 15 天未重启,非断电)。
- **CLI npm 包**:`pnpm --filter @open-design/daemon cli-pack` → `workbuild-cli-0.8.1.tgz`(1.7MB 单文件捆绑,双命令 workbuild/od)。mac 临时前缀装测 ✓(monitor list/cost 真打);win `npm install -g` 装测 ✓(列出 win 自己 base 的监控行,双机隔离可见)。tgz 已入交付文件夹。

### 2026-07-19 CLI npm 包 ESM 打包 bug 根治(客户机实炸回报)
- **炸法**:瘦包 cli.mjs(ESM)里 esbuild 把 CJS 依赖的 require(Node 内置模块)转成 __require 垫片,ESM 无全局 require → "Dynamic require of node:events is not supported"。客户端命令(help/monitor/studio)走纯 fetch 碰不到,之前抽测全绿漏掉;裸 `workbuild`/`daemon start`(懒加载服务端)必炸——客户机首报。
- **修法(build-cli-package.ts)**:①banner 注入 `createRequire` 定义顶层 require(esbuild ESM 标准配方,导入名加前缀避开 bundle 内依赖 hoist 的同名);②define WB_THIN_CLI,cli.ts 把「自起 daemon」意图改为人话引导(瘦包本就不带 sqlite/blake3-wasm 等服务端依赖,产品模型=桌面端供 daemon、CLI 纯客户端),--help 放行。
- **实证**:mac+win 双机重装后四路径全过(--help 正常/裸启动人话引导/daemon start 引导/monitor list 真打)。交付文件夹与打包机 tgz 均已更新;客户机上第三方热修(直接改装好的 cli.mjs)将被本次重装覆盖,行为一致。

### 2026-07-19 「筛不到爆款」+「XHS图片注入失败」双修(用户报,翟总分支同修)
- **筛选逻辑审查结论**:降档阶梯/auto/规则兜底逻辑本身正确;"有时筛不到"真因=采集端归零(窄时间窗 1d/7d 直传抖音搜索 API 掐死冷词;xhs 图文台硬过滤归零),降档无米下锅。
- **修复(tikhub_client.py,两分支同补丁)**:抖音首搜<5条且窄窗→自动放宽180天档补采合并(精确窗口仍由 criteria 二筛);xhs 指定类型归零→综合重搜再滤目标类型。实证:冷词+1天窗 0条→保底触发→候选30→出8条。
- **XHS 图片注入**:desktop 注入桥 `f.startsWith("/")` 校验绝对路径,win 的 C:\ 路径全被滤光→报"缺少 webview id 或文件绝对路径"。修为 `path.isAbsolute`。
- 落地:煜之禾 1f89a28a;翟总 72fbd43d(直推 GitHub;中转仓实为翟总工作检出,拒收当前分支推送,合理);win 客户包已重打(license+引擎修复均已验在包内),交付夹 exe 已替换,客户机重装即修。
- 附带事故:此前为修 claude 设的 npm 全局 allow-scripts 键毒死 tools-pack 构建(npm 11.16 拒绝项目级出现),已删;claude 升级变壳时手动 node install.cjs。

### 2026-07-19 平台纯净性硬约束落地(用户定死:各平台爆款只源于本平台)
- **审计结论**:采集层各平台各走各端点、rc.platform 正确标注、UI 串台修复(XHS6)双分支都在;**真缺陷在降档统一池**:①兜底把抖音播放数与小红书点赞数同池排序,数值上抖音必然霸榜;②全局档位判定让强势平台高档凑满即收工,弱势平台自己档位的货被整体挤出。
- **修复(pipeline.py 双分支)**:_apply_tier_ladder 改为按 rc.platform 分组、各平台均分 target 份额(至少1条)独立走 _ladder_one 再合并;单平台行为不变。实测:「职场沟通」双平台 → 档位「douyin:大爆款 / xiaohongshu:爆款」各自独立,产出 3 小红书 + 5 抖音。
- 落地:煜之禾 11d03c94;翟总 b9f2d469(GitHub 直推);win 已重打+原地更新(数据无损:license/监控行/密钥全保留;发现旧实例未被 stop 杀净导致 resync 不触发,taskkill 树杀后重启,运行时引擎 _ladder_one×4 确认);mac 交付 dmg 已重打替换(包内验证 _ladder_one×4 + license)。

### 2026-07-19 统一创作台移植(用户:翟总分支的视频创作台=理想形态,完整搬来,不含制作视频)
- **移植内容**:创作中枢 StudioCreateView(选平台单选+形态→TikHub真抓爆款/AI选题→候选一键「去创作」→NoteStudioView/ShortVideoStudioView 以 embedded 模式零跳页展开→发布步一稿多发 distribute);选题池 schema 升级(sourceContent/sourceImages/sourcePlatform);配音音色预设+voice-design;双类参考图生图(产品图+风格图,选参考图时内置风格自动让位);web-tabs 运行时+导航 IA(平台入口收进创作中枢)。
- **排除**:MakeVideoView 页面+导航按钮(制作视频模块);视频台「上传」页签内嵌的数字人口型替换按用户"完整拷贝"语义保留。
- **兼容策略(要害)**:以共同祖先三方合并,逐文件审计"翟总删除的我方特性"并全数恢复——飞书双写(providers/store/db/routes/app-config 的 feishuBitableUrl/知识 record 回写/push-review/sync-feishu/监控&系统配置路由)、LARK_PROFILE 注入、feishuSynced 上报、心跳长任务、win 自启、风格系统+实时提示词预览(冲突处保留翟总 refImage 让位逻辑+我方 FinalPromptPreview);并吸收翟总的 bakuanKeysEnv(媒体设置 key 注入引擎,补 volcengine→ARK)。
- **实测(baochuang 打包版)**:typecheck/guard 0 错;导航=文章/创作/知识库/账号/集成(制作视频按钮无);创作页渲染平台 chips+形态+真抓爆款;选题池 POST/GET 落库带新字段;distribute 路由活(缺稿 404 语义正确);fetch-source 200;license valid。

### 2026-07-20 视频号登录深度排查(真Chrome对照,决定性证据)
- **用户确实有视频号「是茂宇呀」(isMasterFinder主账号)**——playwright 真实 Chromium 登录后 auth_finder_list 返回 errCode:0 + finderList:[{nickname:"是茂宇呀",adminNickname:"是茂宇呀（AI自动化）"}]。
- **爆创内嵌 Electron 同接口返回 errCode:300334 "request failed"**。同微信、同接口,真Chrome拿到视频号、爆创拿到300334 → 100%爆创内嵌浏览器问题,非账号问题。
- 已排除:①Client Hints(已补Google Chrome,登录弹窗已从"没有可登录"→"微信快捷登录+显示账号头像",auth_with_oauth errCode:0登录认证通过)。②账号侧(真Chrome证明有视频号)。
- 真Chrome的 auth_finder_list 请求头参考:带 referer=/platform、content-type=application/json、POST body含 scene:7/reqScene:7。**待修**:爆创webview发auth_finder_list时缺某头/referer/body字段→微信拒(300334)。下一步:主进程onSendHeaders抓爆创页面自发的auth_finder_list完整头,与真Chrome逐字段对比补齐。

### 2026-07-20 视频号登录彻底修复(B计划成功)
- **根因(真机对照真Chrome抓出)**:微信视频号助手 auth_finder_list 校验 Referer 必须是后台页 /platform。爆创内嵌webview登录时停在login.html/根路径,发出Referer=根路径→微信拒errCode 300334"request failed"→拉不到视频号列表→卡「没有可登录的视频号」死循环。真Chrome同请求Referer=/platform→errCode:0返回视频号。
- **修复(embedded-browser.ts onBeforeSendHeaders,f3a61187)**:对channels.weixin mmfinderassistant请求规范Referer=/platform。实测:普通fetch(走网络层修复)auth_finder_list 300334→errCode:0返回视频号「是茂宇呀」;视频号webview从login.html跳到/platform后台,完整加载(视频29个/内容管理/发表视频)。接续Client Hints修复(e9d8597a),视频号登录链路彻底打通。
- **附带发现的打包坑**:tools-pack mac install 复用旧app bundle——builder产物(新)已更新但install目录是旧的,导致desktop改动看似不进包(反复grep install目录的旧packaged-main.mjs)。需`rm -rf install目录`强制重装。这解释了排查中"改动进不了包"的假象。
- **待办**:win客户包下次重打带上Referer+ClientHints两个视频号修复;本机测试node已切24(仓库要求)。

### 2026-07-20 视频号升级全自动存草稿(用户断言"可以自动传"被证实)
- 用户点破:之前"微信拒绝CDP传视频"结论是 Client Hints/Referer 修复**之前**的现象——环境伪装修好后重测,CDP setFiles 注入视频被微信正常接受(上传/转码/出封面预览全通)。
- 途中撞出上一轮 Referer 修复的**过度覆盖反噬**:全量强改 `/platform` 导致写接口 `post_draft` 300002(fetch/XHR 间谍抓包实锤)。收窄为只救「坏 Referer」(login.html/裸根路径),业务页真实 Referer 原样保留——登录修复保住,保存草稿放行。
- 新发现:视频号发表页**有「保存草稿」按钮**、后台有「草稿箱」(旧结论"无草稿箱"过时)。适配器升级四步全自动:确认制上传(setFiles 重试到 input 出现+轮询视频预览/「删除」按钮=真传完)→ 短标题按微信字符白名单清洗(`·` 等标红字符换空格)→ 真人 sendInputEvent 点「保存草稿」→ 「将此次编辑保留?」守卫弹窗自动点「保存」;失败时捞页面红字错误上报。铁律不变:绝不点「发表」。
- handoff 白名单补 `tencent`(视频号)/`bilibili`(UI/CLI 双轨规则:UI 能存 CLI 也必须能)。
- **生产命令端到端实证**:`od studio handoff <文章id> --target tencent --account 茂宇` → 1/4 自动上传 → 2/4 填短标题+描述 → 3/4 真人点保存草稿 → 4/4 完成;微信后台「草稿箱(2)」出现《场面话speedrun·视频号真测》2026-07-20 16:16 带视频缩略图(截图铁证)。
- 途中排坑:哑成片 final.mp4 仅 7.5K(时长不够会被拒)→ 换真实 2MB 成片;发表页新版**表单常显**,"表单出现=上传完成"旧判定失效 → 改视频预览/「删除」按钮判定。
